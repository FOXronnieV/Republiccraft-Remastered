plugins { id 'java' }
java { sourceCompatibility = JavaVersion.VERSION_25; targetCompatibility = JavaVersion.VERSION_25 }
dependencies { compileOnly 'org.teavm:teavm-jso-apis:0.14.1' } // Để GLFW và GL11 dùng được @JSBody